# 開発ログ

## 2026-05-08: 診断フォームのバリデーション改善と品質向上
- ステップ別バリデーションを実装（現在のステップが完了しないと次へ進めない制御）
- 会社名を必須化（個人利用の場合は「個人」と入力するガイドを追加）
- 「その他」選択時の条件付き必須入力を実装（ZodのsuperRefineを使用）
- やさしいエラーデザインを追加（オレンジ背景、アイコン、ソフトな文言）
- エラー発生時の該当項目への自動スクロールを実装
- Step 4（業務入力）で業務0件時の空状態UIを追加
- 業務一覧の下に大きな「＋ 業務を1つ追加する」導線を追加
- 業務候補チップからの追加時にカテゴリを自動分類する機能を改善
- 業務詳細に「成果物」「使用ツール」「困りごと」「その他メモ」を追加（複数選択チップ形式）
- Step 5（確認画面）で全ステップの未入力チェックと診断ボタンの有効/無効制御を実装
- API連携、DB連携、レポート画面はまだ未実装

## 2026-05-08: レポート表示画面と診断エンジンの実装
- 診断結果表示画面 (`/report`) を新規実装
- 診断結果生成エンジン (`src/lib/reportGenerator.ts`) を新規作成
- 診断ロジック (`src/lib/calculations.ts`) を更新（削減率・スコアリングの最新化）
- 診断完了時に結果を `localStorage` に一時保存して `/report` へ遷移する機能を実装
- Recharts を用いたデータ可視化（業務別削減時間の棒グラフ、ポテンシャル分析のレーダーチャート）を実装
- 業務カテゴリ別の個別アドバイス、品質向上ポイント、注意点、プロンプト例の自動生成を実装
- KPIカード（月間・年間削減時間、平均削減時間など）を実装
- 削減効果が高い順のランキング表示を実装
- 慎重な判断が必要な業務（機密情報あり等）の抽出と警告表示を実装
- 全体的なアクションプランと免責事項の表示を実装
- 印刷（PDF保存）を考慮したレイアウト調整
- OpenAI API、Supabase、Neon はまだ未実装（モック診断）

## 2026-05-08: ビルドエラーの修正と型定義の厳格化
- `npm run build` 時の TypeScript 型エラーを修正
- `src/app/diagnose/page.tsx` の Zod スキーマを修正し、配列項目が `undefined` にならないよう厳格化
- `useForm` の `defaultValues` を補完し、スキーマと実データの型を完全に同期
- `src/app/report/page.tsx` でのアイコンインポート漏れ (`BarChart3`, `Briefcase`, `Target`) を修正
- `recharts` の `BarChart` と `lucide-react` の `BarChart3` の名称衝突を別名インポートで解消

## 2026-05-08: レポート画面の品質改善と実務対応化
- レポート画面 (`/report`) のUIを大幅に刷新し、実務利用レベルの品質へ向上
- 診断エンジンを強化し、業務ごとの「AIの役割」と「人間の役割」を具体的に提示する機能を追加
- タイトルやカテゴリに基づく「ガバナンス上の注意が必要な業務」の自動抽出機能を実装
- ヒーローセクションに円形スコアゲージとKPIサマリーを配置し、視認性を大幅に改善
- M365 Copilot や Gemini for Workspace 等、AIツールのプラン特性に応じた注意表示を追加
- プロンプト例、品質向上ポイント、注意点、アクションプランの表示レイアウトを改善
- レポート末尾に詳細な免責事項（Disclaimer）セクションを追加
- モバイル表示およびPDF印刷（印刷用レイアウト）への最適化を実施
- `src/lib/types.ts` を拡張し、新機能に対応する型定義を追加
- 全てのアイコンインポートと型定義を整合させ、`npm run build` が通る状態を維持

## 2026-05-08: レポート画面の日本語化と初心者向け最適化
- 主要ラベルを英語から日本語へ統一し、実務での可読性を向上
- 表紙エリアの情報を整理（氏名、業種、会社規模、役職、診断日、対象ツール、プランを分離表示）
- 選択されたAIツール（ChatGPT, Copilot, Gemini）に応じて注意文やアクションを動的に切り替えるロジックを実装
- 業務別ガイドに「入力してはいけない情報（機密・個人情報等）」を追加し、セキュリティ配慮を強化
- 5段階評価のスコア（AI活用ポテンシャル）に対する日本語の解説と注釈を追加
- 総合診断アドバイスの文章を自然で分かりやすい日本語のトーンに修正
- 免責事項を「ご利用上の注意」としてリデザインし、視認性を大幅に改善
- `src/lib/types.ts` および `src/lib/reportGenerator.ts` に `doNotInput` 項目を追加
- 全ての修正において `npm run build` が通る状態を維持

## 2026-05-09: レポート画面のユーティリティ強化とレイアウト改善
- レポート最下部に「保存・共有セクション」を新設し、PDF保存および共有リンクコピー機能を実装
- 各業務のプロンプト例にコピーボタンを追加し、操作性を向上
- 印刷（PDF出力）時に不要なボタン類（共有・コピー等）を自動で非表示にする調整を実施
- 表紙の会社情報エリアを2列グリッド化し、日本語ラベルを強調して視認性を向上
- 月間・年間削減時間（KPI）付近に予測値である旨の注釈を追加
- 業務別ランキングをレスポンシブ対応させ、スマホ表示では縦積みカード形式へ変換
- 業務別ガイドにおすすめAIツールの選定理由を表示する機能を追加
- 「ご利用上の注意」のデザインを強調し、ガバナンス面の配慮を強化
- 全ての修正において `npm run build` が通る状態を維持

## 2026-05-09: 診断フォームの遷移フロー修正とUI日本語化
- Step 4（業務入力）から Step 5（最終確認）へ遷移せず直接レポート画面へ飛ぶ不具合を修正
- Step 5（最終確認画面）を新設し、診断前に全入力内容をプレビューできる機能を追加
- 各ステップの入力内容を修正するための戻り導線を Step 5 に配置
- 診断レポートの生成および localStorage 保存を Step 5 の完了時のみに限定
- Step 4 の「業務を追加する」ボタンのデザインを強化し、操作性を向上
- Step 2 のAIツールカードに具体的なユースケース（タグ）を追加し、選択をサポート
- ステップ表示、進捗率、ボタン等の主要ラベルを英語から日本語へローカライズ
- フォーム内での Enter キー押下による意図しないサブミットを防止する処理を追加
- TypeScript の型定義と Zod スキーマを整合させ、`npm run build` が通る状態を維持

## 2026-05-09: 診断フォームの重大な不具合修正（Step 5 確認画面の復元）
- Step 4（業務入力）から Step 5 をスキップして直接 `/report` へ遷移していた不具合を修正
- Step 5（確認画面）を復元し、基本情報・ツールプラン・業務一覧のサマリーを表示
- Step 5 に各ステップ（1〜4）へ戻るための「修正する」導線を追加
- レポート生成処理を Step 5 の提出時のみに限定し、提出プロセスの安全性を確保
- Step 1〜4 のボタンを `type="button"` に、Step 5 のみ `type="submit"` に設定し、submit の暴発を防止
- 5ステップ構成（20%完了刻み）を維持
- Step 1 および Step 4 の全入力項目（部署名、成果物、メモ等）が正しく保持・プレビューされるよう修正
- フォーム内での Enter キーによる意図しないレポート生成を防止
- `npm run build` が正常に通る状態を維持

## 2026-05-09: 診断フォームの根本修正とUI/UX改善
- `form onSubmit` と `type="submit"` を廃止し、`div` と `handleGenerateReport` による手動遷移へ変更（意図しない遷移を完全に防止）
- Step 1 の「その他」入力欄（役職、使用ツール、期待すること）を復元し、バリデーションを強化
- Step 4 の「その他」入力欄（成果物、使用ツール、困りごと）を復元し、バリデーションを強化
- Step 5 確認画面の英語ラベル（FINAL CHECK, Time, Freq 等）を完全に日本語化
- 未選択項目の表示を「-」から「未選択」へ改善し、視認性を向上
- AIツール選択（Step 2）のUIを改善（選択中ラベル、チェックアイコンの追加、複数選択の明記）
- プラン選択（Step 3）にツール別の注意文（Copilot/Gemini）を追加
- 任意項目に「入力すると診断精度が上がる」旨のガイドを追加
- `npm run build` が正常に通る状態を維持

## 2026-05-09: Supabase連携の基盤実装（第一段階）
- `@supabase/supabase-js` をインストール
- `src/lib/supabase.ts`: Supabase クライアントの初期化設定（環境変数のフォールバック付）
- `src/lib/reportStorage.ts`: DB保存・取得および localStorage フォールバックの抽象化
- `src/lib/types.ts`: `DiagnosisRecord` 型の定義追加
- `docs/supabase-setup.sql`: テーブル設計と RLS ポリシー（INSERT/SELECT許可）を定義
- `.env.example`: 環境変数のテンプレートを作成
- `npm run build` が正常に通ることを確認

## 2026-05-09: 診断結果の保存機能と共有URLの実装
- `src/components/report/ReportView.tsx`: レポート表示UIをコンポーネント化し、`/report` と `/report/[shareId]` で共通利用可能に
- `src/app/diagnose/page.tsx`: 診断完了時に Supabase へデータを保存し、成功時は `share_id` 付きの URL へリダイレクトするよう修正
- `src/app/report/page.tsx`: `localStorage` からの読み込みに限定し、表示を `ReportView` に委譲
- `src/app/report/[shareId]/page.tsx`: Supabase から ID 指定でデータをフェッチして表示する共有ページを新規作成
- `src/lib/supabase.ts`: ビルド時の環境変数不足によるクラッシュを防ぐため、初期化ロジックを堅牢化
- `ReportView`: 共有状態（`shareId` の有無）に応じて、共有ボタンの挙動とメッセージを動的に変更する機能を実装
- `npm run build` が正常に通ることを確認

## 2026-05-09: 共有URL遷移の不具合修正とドキュメント整備
- `src/lib/reportStorage.ts`: `insert` 後の `select().single()` 依存を廃止し、生成済み `share_id` を即座に返す設計に変更
- `src/app/diagnose/page.tsx`: 保存処理の成功判定 (`success`) を導入し、成功時は確実に `/report/[shareId]` へ遷移するよう修正
- `src/components/report/ReportView.tsx`: ローカル表示時の警告文をより具体的に修正（`rpt_...` の必要性を明記）
- `src/lib/types.ts`: 保存結果を扱う `SaveDiagnosisResult` 型を追加
- `docs/obsidian/`: プロジェクト状況と設計仕様をまとめた Obsidian 用の開発メモを作成
- Obsidian Vault (`OneDrive/Documents/Obsidian Vault/ai-gyomu-diagnosis-report`) への自動同期を実装
- `scripts/sync-obsidian.js` を作成し、`npm run sync:obsidian` でドキュメントを同期可能に
- `/admin`: 診断結果一覧を表示する管理ダッシュボードを実装（KPI集計、検索、フィルター機能付き）
- `/admin`: CSVエクスポート機能を追加（フィルター結果連動、UTF-8 BOM付き）
- `/admin`: 簡易パスワード認証を実装（環境変数 `NEXT_PUBLIC_ADMIN_PASSWORD` を使用、sessionStorage でセッション維持）
- `docs/vercel-deploy-checklist.md`: Vercel 公開前の環境変数・セキュリティ・動作確認項目を整理
- `src/lib/reportStorage.ts`: 管理者用データ取得関数 `getAllReports` を追加
- Vercel への初回デプロイ完了と本番環境での動作確認
    - `/diagnose` からの診断・Supabase保存・リダイレクトの成功を確認
    - `/report/[shareId]` による共有レポート表示の成功を確認
    - `/admin` の簡易パスワード認証および KPI 集計、CSV 出力の動作を確認
- `src/app/page.tsx`: Next.js デフォルト画面を削除し、サービスの魅力を伝えるランディングページ（LP）として刷新
- `npm run build` が正常に通ることを確認
